export interface user {
  _id: string;
  authId: string;
  provider: string;
  username: string;
  email: string;
  picture: string;
  // name: string;
  // lastName: string;
  // companyRoles: { company: company; role: role }[];
}
