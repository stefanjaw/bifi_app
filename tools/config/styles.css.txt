/* You can add global styles to this file, and also import other style files */
/* You can add global styles to this file, and also import other style files */
@import "tailwindcss";

/* PrimeNG tailwind plugin */
@plugin "tailwindcss-primeui";
@import "primeicons/primeicons.css";

/* TailwindCSS: Style asset roaster library */
/* @source "../../../dist/asset-roaster/"; */
/* @source "@avalantec/asset-roaster"; */

/* TailwindCSS: Style base app library */
/* @source "../../../dist/base-app/"; */
/* @source "@avalantec/base-app"; */

html,
body {
  height: 100%;
}
body {
  margin: 0;
  font-family: "Helvetica Neue", sans-serif;
}

:root {
  --toolbar-color: #1a1f3b;
}

/* Dense Data – Global Stylesheet
 * Compact controls for data-heavy applications.
 * Maps the Dense Data design token set to PrimeNG components and global elements.
 */

/* ─── Body / Container ────────────────────────────────────────────────────── */
body {
  background-color: #f3f4f6; /* gray-100 */
  color: #111827;            /* gray-900 */
}

/* ─── Dense Design Token CSS Variables ───────────────────────────────────── */
:root {
  --dense-text-size: 0.75rem;         /* text-xs  = 12px */
  --dense-text-color: #1f2937;        /* gray-800 */
  --dense-text-muted-size: 0.6875rem; /* 11px     */
  --dense-text-muted-color: #6b7280;  /* gray-500 */

  --dense-card-bg: #ffffff;
  --dense-card-border: #d1d5db;       /* gray-300 */
  --dense-card-radius: 2px;           /* rounded-sm */

  --dense-btn-radius: 2px;
  --dense-btn-padding-x: 0.625rem;    /* px-2.5 = 10px */
  --dense-btn-padding-y: 0.25rem;     /* py-1   =  4px */
  --dense-btn-font-size: 0.75rem;     /* text-xs */

  --dense-input-border: #9ca3af;      /* gray-400 */
  --dense-input-radius: 2px;
  --dense-input-padding-x: 0.5rem;   /* px-2   =  8px */
  --dense-input-padding-y: 0.25rem;  /* py-1   =  4px */
  --dense-input-font-size: 0.75rem;  /* text-xs */
}

/* ─── PrimeNG CSS Variable Overrides – Button ─────────────────────────────── */
/* These override the generated PrimeNG design-token CSS variables so every
   button variant (primary, text, link, outlined, etc.) inherits compact sizing */
:root {
  --p-button-border-radius: 2px;
  --p-button-padding-x: 0.625rem;
  --p-button-padding-y: 0.25rem;
  --p-button-font-size: 0.75rem;
  --p-button-sm-font-size: 0.6875rem;
  --p-button-sm-padding-x: 0.5rem;
  --p-button-sm-padding-y: 0.1875rem;
  --p-button-lg-font-size: 0.875rem;
  --p-button-lg-padding-x: 0.75rem;
  --p-button-lg-padding-y: 0.375rem;
}

/* ─── PrimeNG CSS Variable Overrides – Input ──────────────────────────────── */
:root {
  --p-inputtext-border-radius: 2px;
  --p-inputtext-padding-x: 0.5rem;
  --p-inputtext-padding-y: 0.25rem;
  --p-inputtext-font-size: 0.75rem;
  --p-inputtext-border-color: #9ca3af; /* gray-400 – Dense Data inputBase token */
  --p-select-border-radius: 2px;
  --p-multiselect-border-radius: 2px;
  --p-autocomplete-border-radius: 2px;
}

/* ─── PrimeNG Buttons – compact sizing (fallback class overrides) ─────────── */
.p-button {
  padding: var(--dense-btn-padding-y) var(--dense-btn-padding-x);
  font-size: var(--dense-btn-font-size);
  border-radius: var(--dense-btn-radius);
  gap: 0.375rem;
}

.p-button .p-button-icon {
  font-size: var(--dense-btn-font-size);
  width: 0.75rem;
  height: 0.75rem;
}

/* ─── PrimeNG Inputs – compact sizing + gray-400 border ───────────────────── */
.p-inputtext {
  padding: var(--dense-input-padding-y) var(--dense-input-padding-x);
  font-size: var(--dense-input-font-size);
  border-radius: var(--dense-input-radius);
  border-color: var(--dense-input-border);
}

.p-textarea {
  padding: var(--dense-input-padding-y) var(--dense-input-padding-x);
  font-size: var(--dense-input-font-size);
  border-radius: var(--dense-input-radius);
  border-color: var(--dense-input-border);
}

/* ─── PrimeNG Select / Dropdown – compact sizing + gray-400 border ────────── */
.p-select,
.p-multiselect,
.p-autocomplete {
  border-radius: var(--dense-input-radius);
  border-color: var(--dense-input-border);
}

.p-select .p-select-label,
.p-multiselect .p-multiselect-label,
.p-autocomplete .p-autocomplete-input,
.p-datepicker-input {
  padding: var(--dense-input-padding-y) var(--dense-input-padding-x);
  font-size: var(--dense-input-font-size);
}

.p-select-dropdown,
.p-multiselect-dropdown {
  width: 1.75rem;
}

/* ─── PrimeNG Checkbox & Radio – compact sizing ───────────────────────────── */
.p-checkbox .p-checkbox-box {
  width: 0.875rem;   /* h-3.5 */
  height: 0.875rem;
  border-radius: 2px;
  border-color: var(--dense-input-border);
}

.p-radiobutton .p-radiobutton-box {
  width: 0.875rem;
  height: 0.875rem;
  border-color: var(--dense-input-border);
}

/* ─── PrimeNG Datatable – dense rows ─────────────────────────────────────── */
.p-datatable .p-datatable-thead > tr > th,
.p-datatable .p-datatable-tbody > tr > td {
  padding: 0.375rem 0.625rem;
  font-size: var(--dense-text-size);
}

/* ─── PrimeNG Dialog – compact header ────────────────────────────────────── */
.p-dialog .p-dialog-header {
  padding: 0.625rem 1rem;
}

.p-dialog .p-dialog-content {
  padding: 0.75rem 1rem;
}

.p-dialog .p-dialog-footer {
  padding: 0.5rem 1rem;
}

/* ─── PrimeNG Panel / Card – compact padding ─────────────────────────────── */
.p-panel .p-panel-header {
  padding: 0.5rem 0.75rem;
  font-size: var(--dense-text-size);
}

.p-panel .p-panel-content {
  padding: 0.5rem 0.75rem;
}

.p-card .p-card-content {
  padding: 0.5rem 0;
}

/* ─── PrimeNG Menu / Listbox – compact items ─────────────────────────────── */
.p-menu .p-menu-item-link,
.p-menubar .p-menubar-item-link {
  padding: 0.375rem 0.625rem;
  font-size: var(--dense-text-size);
}

/* ─── PrimeNG Tabs – compact tabs ────────────────────────────────────────── */
.p-tabs .p-tab {
  padding: 0.375rem 0.75rem;
  font-size: var(--dense-text-size);
}

/* ─── PrimeNG Toggle Switch – compact ────────────────────────────────────── */
.p-toggleswitch {
  width: 1.75rem;   /* w-7 */
  height: 1rem;     /* h-4 */
}

.p-toggleswitch .p-toggleswitch-slider::before {
  width: 0.75rem;   /* h-3 w-3 */
  height: 0.75rem;
}

/* ─── PrimeNG Slider – compact ───────────────────────────────────────────── */
.p-slider .p-slider-handle {
  width: 0.75rem;
  height: 0.75rem;
}

.p-slider.p-slider-horizontal {
  height: 0.25rem;
}

/* ─── PrimeNG Tooltip – compact ──────────────────────────────────────────── */
.p-tooltip .p-tooltip-text {
  font-size: var(--dense-text-size);
  padding: 0.25rem 0.5rem;
}

/* ─── PrimeNG Tag / Badge – compact ──────────────────────────────────────── */
.p-tag {
  font-size: 0.6875rem; /* 11px */
  padding: 0.125rem 0.375rem;
  border-radius: 2px;
}

/* ─── PrimeNG InputGroup – compact ───────────────────────────────────────── */
.p-inputgroup .p-inputgroup-addon {
  padding: var(--dense-input-padding-y) var(--dense-input-padding-x);
  font-size: var(--dense-input-font-size);
}